#ifndef _FUR_ELISE_HPP
#define _FUR_ELISE_HPP

#include "melody.hpp"

class fur_elise : public melody {
public:
   void play( note_player & p ){
   p.play( note{ note::E5, note::dQ } );
   p.play( note{ note::D5s, note::dQ } );
   p.play( note{ note::E5, note::dQ } );
   p.play( note{ note::D5s, note::dQ } );
   p.play( note{ note::E5, note::dQ } );
   p.play( note{ note::B4, note::dQ } );
   p.play( note{ note::D5, note::dQ } );
   p.play( note{ note::C5, note::dQ } );
   p.play( note{ note::A4, note::dH } );
    }
};

#endif   p.play( note{ note::C5, note:: } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::F5, note:: } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::A5, note:: } );
   p.play( note{ note::, note:: } );
   p.play( note{ note::, note:: } );
   p.play( note{ note::A5, note::dQ } );
   p.play( note{ note::, note:: } );
   p.play( note{ note::G5, note::dH } );
   p.play( note{ note::C5, note:: } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::F5, note:: } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::G5, note:: } );
   p.play( note{ note::, note:: } );
   p.play( note{ note::, note:: } );
   p.play( note{ note::G5, note::dQ } );
   p.play( note{ note::, note:: } );
   p.play( note{ note::F5, note::dQ } );
   p.play( note{ note::E5, note:: } );
   p.play( note{ note::D5, note::dQ } );
   p.play( note{ note::C5, note:: } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::F5, note:: } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::F5, note::dH } );
   p.play( note{ note::G5, note::dQ } );
   p.play( note{ note::E5, note::dQ } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::C5, note::dQ } );
   p.play( note{ note::, note::dQ } );
   p.play( note{ note::C5, note::dQ } );
   p.play( note{ note::G5, note::dQ } );
   p.play( note{ note::, note::dQ } );
   p.play( note{ note::F5, note::dF } );
   p.play( note{ note::C5, note:: } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::F5, note:: } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::A5, note:: } );
   p.play( note{ note::, note:: } );
   p.play( note{ note::, note:: } );
   p.play( note{ note::A5, note::dQ } );
   p.play( note{ note::, note:: } );
   p.play( note{ note::G5, note::dH } );
   p.play( note{ note::C5, note:: } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::F5, note:: } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::, note::dH } );
   p.play( note{ note::E5, note::dQ } );
   p.play( note{ note::F5, note::dQ } );
   p.play( note{ note::E5, note:: } );
   p.play( note{ note::D5, note::dQ } );
   p.play( note{ note::C5, note:: } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::F5, note:: } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::F5, note::dH } );
   p.play( note{ note::G5, note::dQ } );
   p.play( note{ note::E5, note::dQ } );
   p.play( note{ note::D5, note:: } );
   p.play( note{ note::C5, note::dQ } );
   p.play( note{ note::, note::dQ } );
   p.play( note{ note::C5, note::dQ } );
   p.play( note{ note::G5, note::dQ } );
   p.play( note{ note::, note::dQ } );
   p.play( note{ note::F5, note::dF } );
    }
};

#endif